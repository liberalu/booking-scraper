# AGENTS.md

See **[CLAUDE.md](CLAUDE.md)** — it is the single source of truth for this
repository's commands, architecture and conventions.

This file used to be a shortened copy of it and had drifted badly: it still
described a Scrapy project whose only shop was vaga.lt, with a `listings` table
that had been renamed to `shop_books` long before, and commands for a stack that
no longer exists. A second copy of instructions is worse than none, because
whichever one you happen to read is probably the stale one.
