#!/usr/bin/env php
<?php

declare(strict_types=1);

$arguments = array_slice($argv, 1);
$command = [PHP_BINARY, dirname(__DIR__).'/artisan', 'crawler:parse', ...$arguments];
passthru(implode(' ', array_map(escapeshellarg(...), $command)), $status);
exit($status);
