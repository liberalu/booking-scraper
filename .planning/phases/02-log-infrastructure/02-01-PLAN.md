---
phase: 02-log-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements: [LOGINFRA-01]

must_haves:
  truths:
    - "`docker compose up -d` brings up loki, promtail, grafana alongside the existing services without manual setup"
    - "Grafana declares a healthcheck (Alpine image, has wget); Loki and Promtail don't (Distroless images) — readiness is verified by Plan 02-03's smoke test via curl from the host"
    - "Promtail can read `/var/run/docker.sock`, `/var/lib/docker/containers/`, and the `scraper_logs` volume (all read-only)"
    - "Grafana can read its provisioning directory mounted from `monitoring/grafana/provisioning/`"
    - "All three services join the default compose network so they reach each other by service name (`loki:3100`, `grafana:3000`, etc.)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Loki, Promtail, Grafana services with healthchecks, volumes, and dependencies"
      contains: |
        services with names `loki`, `promtail`, `grafana`; new named volumes `loki_data` and `grafana_data`
  key_links:
    - from: "docker-compose.yml `promtail` service"
      to: "/var/run/docker.sock, /var/lib/docker/containers/, and scraper_logs volume"
      via: "volumes:\n  - /var/run/docker.sock:/var/run/docker.sock:ro\n  - /var/lib/docker/containers:/var/lib/docker/containers:ro\n  - scraper_logs:/var/log:ro\n  - ./monitoring/promtail:/etc/promtail:ro"
      pattern: "/var/lib/docker/containers:/var/lib/docker/containers:ro"
    - from: "docker-compose.yml `grafana` service"
      to: "./monitoring/grafana provisioning + dashboards"
      via: "volumes:\n  - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro\n  - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro\n  - grafana_data:/var/lib/grafana"
      pattern: "/etc/grafana/provisioning:ro"
---

<objective>
Add the Loki / Promtail / Grafana observability stack to the existing `docker-compose.yml` so that `docker compose up -d` brings the full stack up alongside postgres / scraper / dashboard / flaresolverr. No build step — all three services use upstream official images.

This plan delivers the compose-level scaffolding (services + volumes + healthchecks + mounts). Config file content (Loki retention, Promtail scrape jobs, Grafana provisioning) lives in **plan 02-02** and **plan 02-03** — those configs are *read by* the containers this plan brings up. The mount paths declared here MUST match the paths plan 02-02 / 02-03 write to.
</objective>

<execution_context>
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-log-infrastructure/02-CONTEXT.md
@CLAUDE.md
@docker-compose.yml

<interfaces>
Existing `docker-compose.yml` (verbatim shape — preserve all of it):

```yaml
services:
  postgres:           # port 5432, healthcheck pg_isready
  postgres-test:      # port 5433, profile: test
  flaresolverr:       # port 8191
  scraper:            # depends_on: postgres healthy + flaresolverr started; mounts scraper_logs:/var/log
  dashboard:          # port 8000; mounts /var/run/docker.sock + scraper_logs:/var/log:ro
volumes:
  pgdata:
  pgdata_test:
  scraper_logs:
```

The `dashboard` service is the precedent for `/var/run/docker.sock` mounts (api.py:611 uses it for `containers[].exec_run`). Promtail uses the same socket the same way.

The `scraper_logs` named volume already serves both `scraper` (RW at `/var/log`) and `dashboard` (RO at `/var/log:ro`). Promtail joins this same volume RO at `/var/log:ro` so it can scrape `/var/log/scraper.log` and `/var/log/scrapy_runs/*.log`.

Locked decisions from CONTEXT.md:
- Always-on, single compose file (no profiles, no split files)
- Upstream `:latest` tags — accept defaults for v1.2
- Personal-scale, no resource limits, default network
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monitoring/ directory scaffold so bind mounts resolve when compose runs</name>
  <files>
    monitoring/loki/.gitkeep
    monitoring/promtail/.gitkeep
    monitoring/grafana/provisioning/datasources/.gitkeep
    monitoring/grafana/provisioning/dashboards/.gitkeep
    monitoring/grafana/dashboards/.gitkeep
  </files>
  <read_first>
    - .planning/phases/02-log-infrastructure/02-CONTEXT.md (confirms `monitoring/` is the layout convention)
  </read_first>
  <action>
Create the empty directory structure under `monitoring/` at the repo root so the bind mounts declared in Task 2 resolve cleanly even before plans 02-02 / 02-03 have written any configs. Without these directories, the first `docker compose up -d` after Task 2 lands errors with "no such file or directory" on the bind mounts.

Commands (run from repo root):

```bash
mkdir -p monitoring/loki
mkdir -p monitoring/promtail
mkdir -p monitoring/grafana/provisioning/datasources
mkdir -p monitoring/grafana/provisioning/dashboards
mkdir -p monitoring/grafana/dashboards
touch monitoring/loki/.gitkeep
touch monitoring/promtail/.gitkeep
touch monitoring/grafana/provisioning/datasources/.gitkeep
touch monitoring/grafana/provisioning/dashboards/.gitkeep
touch monitoring/grafana/dashboards/.gitkeep
```

The `.gitkeep` files keep the directories tracked while they're empty. Plans 02-02 / 02-03 populate them with real configs; the `.gitkeep` files stay alongside (harmless) or can be deleted once siblings exist.

**Why this is Task 1, not Task 2:** Task 2 edits docker-compose.yml. If the executor (or operator) runs `docker compose up -d` between Task 2 and a not-yet-run dir-creation step, compose errors. Dir-creation first means Task 2's edits are immediately safe to apply.
  </action>
  <verify>
    <automated>
[ -d monitoring/loki ] &&
[ -d monitoring/promtail ] &&
[ -d monitoring/grafana/provisioning/datasources ] &&
[ -d monitoring/grafana/provisioning/dashboards ] &&
[ -d monitoring/grafana/dashboards ] &&
[ -f monitoring/loki/.gitkeep ] &&
[ -f monitoring/promtail/.gitkeep ] &&
[ -f monitoring/grafana/provisioning/datasources/.gitkeep ] &&
[ -f monitoring/grafana/provisioning/dashboards/.gitkeep ] &&
[ -f monitoring/grafana/dashboards/.gitkeep ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `find monitoring -type d | sort` lists exactly: `monitoring`, `monitoring/grafana`, `monitoring/grafana/dashboards`, `monitoring/grafana/provisioning`, `monitoring/grafana/provisioning/dashboards`, `monitoring/grafana/provisioning/datasources`, `monitoring/loki`, `monitoring/promtail`
    - `find monitoring -name .gitkeep | wc -l` returns 5
    - `git status monitoring/` shows the 5 new `.gitkeep` files as untracked or staged
  </acceptance_criteria>
  <done>Directory tree under `monitoring/` is present with `.gitkeep` placeholders. Compose bind mounts in Task 2 will resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Append loki, promtail, grafana services + new volumes to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (current state — preserve postgres / postgres-test / flaresolverr / scraper / dashboard exactly as they are; only append new services and volumes)
    - .planning/phases/02-log-infrastructure/02-CONTEXT.md (locked decisions, mount-path conventions)
  </read_first>
  <action>
Edit `docker-compose.yml` and **append** three new services to the `services:` block and **append** two new named volumes to the `volumes:` block. Do NOT modify the existing `postgres`, `postgres-test`, `flaresolverr`, `scraper`, or `dashboard` service blocks.

Append the following inside the `services:` block, after the `dashboard:` service (preserve indentation — two-space indent matching the rest of the file):

```yaml
  loki:
    image: grafana/loki:latest
    restart: unless-stopped
    command: -config.file=/etc/loki/loki-config.yml
    ports:
      - "3100:3100"
    volumes:
      - ./monitoring/loki:/etc/loki:ro
      - loki_data:/loki
    # No healthcheck declared — Loki's official image is Distroless (no shell,
    # no wget) so the standard `CMD-SHELL wget /ready` pattern doesn't work.
    # Plan 02-03 smoke test verifies Loki's `/ready` endpoint directly via
    # curl from the host. Other services use `depends_on: service_started`
    # to wait for the container to start (not health).

  promtail:
    image: grafana/promtail:latest
    restart: unless-stopped
    command: -config.file=/etc/promtail/promtail-config.yml
    depends_on:
      loki:
        condition: service_started
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # Host containers dir for the docker_sd_configs __path__ rewrite —
      # Promtail tails the JSON-file logs Docker writes per container.
      # Without this mount the `containers` scrape job in
      # monitoring/promtail/promtail-config.yml resolves __path__ to
      # /var/lib/docker/containers/<id>/<id>-json.log and gets ENOENT
      # inside the Promtail container. On macOS Docker Desktop the path
      # is surfaced from the VM via the bind mount; works the same as
      # /var/run/docker.sock does for the `dashboard` service.
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - scraper_logs:/var/log:ro
      - ./monitoring/promtail:/etc/promtail:ro
    # No healthcheck — Distroless image, same reason as Loki. The Plan 02-03
    # smoke test verifies end-to-end that lines reach Loki, which proves
    # Promtail is functional.

  grafana:
    image: grafana/grafana:latest
    restart: unless-stopped
    depends_on:
      loki:
        condition: service_started
      postgres:
        condition: service_healthy
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_AUTH_ANONYMOUS_ENABLED: "false"
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_INSTALL_PLUGINS: ""
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    healthcheck:
      # Grafana's official image is Alpine-based and has wget. Probe the
      # internal /api/health endpoint — it returns {"database":"ok",...}
      # once the DB connection is established.
      test: ["CMD-SHELL", "wget -q -O- http://localhost:3000/api/health | grep -q '\"database\": \"ok\"'"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 30s
```

Then append two new named volumes inside the existing `volumes:` block at the bottom of the file. The existing block currently has:

```yaml
volumes:
  pgdata:
  pgdata_test:
  scraper_logs:
```

Modify it to read:

```yaml
volumes:
  pgdata:
  pgdata_test:
  scraper_logs:
  loki_data:
  grafana_data:
```

**Notes:**
- Compose service names are DNS-resolvable on the default network — `grafana` reaches Loki at `http://loki:3100` and Postgres at `postgres:5432`. Plans 02-02 and 02-03 rely on those names.
- The bind-mount paths `./monitoring/loki`, `./monitoring/promtail`, `./monitoring/grafana/provisioning`, `./monitoring/grafana/dashboards` are created by Task 1 above.
- **No `wget`-based healthcheck on Loki / Promtail** — both images are Distroless. Authoritative readiness probes live in Plan 02-03's smoke test (curl from the host). Grafana's healthcheck uses wget (Alpine base, has it).
- `GF_INSTALL_PLUGINS: ""` is an explicit anti-default: prevents accidentally pulling plugins on first boot. Keep it.
- `GF_AUTH_ANONYMOUS_ENABLED: "false"` and `GF_USERS_ALLOW_SIGN_UP: "false"` enforce defense-in-depth even on localhost (per CONTEXT.md).
  </action>
  <verify>
    <automated>
yq eval '.services.loki.image' docker-compose.yml | grep -q "^grafana/loki" &&
yq eval '.services.promtail.image' docker-compose.yml | grep -q "^grafana/promtail" &&
yq eval '.services.grafana.image' docker-compose.yml | grep -q "^grafana/grafana" &&
yq eval '.services.promtail.volumes[]' docker-compose.yml | grep -q "/var/lib/docker/containers" &&
yq eval '.services.promtail.depends_on.loki.condition' docker-compose.yml | grep -q "^service_started$" &&
yq eval '.services.grafana.depends_on.loki.condition' docker-compose.yml | grep -q "^service_started$" &&
yq eval '.volumes | keys' docker-compose.yml | grep -q "loki_data" &&
yq eval '.volumes | keys' docker-compose.yml | grep -q "grafana_data" &&
docker compose config --quiet
    </automated>
    <manual>
- Verify with `docker compose config` that the merged file is structurally valid.
- Eyeball the diff: existing services unchanged, new services appended cleanly.
    </manual>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  loki:" docker-compose.yml` returns exactly 1
    - `grep -c "^  promtail:" docker-compose.yml` returns exactly 1
    - `grep -c "^  grafana:" docker-compose.yml` returns exactly 1
    - `grep -c "grafana/loki:latest" docker-compose.yml` returns 1
    - `grep -c "grafana/promtail:latest" docker-compose.yml` returns 1
    - `grep -c "grafana/grafana:latest" docker-compose.yml` returns 1
    - `grep -c "/var/run/docker.sock:/var/run/docker.sock:ro" docker-compose.yml` returns 1 (in promtail)
    - `grep -c "/var/lib/docker/containers:/var/lib/docker/containers:ro" docker-compose.yml` returns 1 (in promtail)
    - `grep -c "scraper_logs:/var/log:ro" docker-compose.yml` returns 2 (dashboard + promtail)
    - `grep -c "loki_data:" docker-compose.yml` returns at least 2 (one in service, one in volumes block)
    - `grep -c "grafana_data:" docker-compose.yml` returns at least 2
    - `grep -c "GF_AUTH_ANONYMOUS_ENABLED" docker-compose.yml` returns 1
    - `grep -c "condition: service_started" docker-compose.yml` returns at least 3 (existing scraper→flaresolverr + new promtail→loki + new grafana→loki)
    - Loki and Promtail service blocks do NOT contain a `healthcheck:` key (grep -A 20 for each service should not match `^    healthcheck:`)
    - `docker compose config --quiet` exits 0
    - Existing services still present: `grep -c "^  postgres:" docker-compose.yml` returns 1, same for `dashboard:`, `scraper:`, `flaresolverr:`, `postgres-test:`
  </acceptance_criteria>
  <done>docker-compose.yml has three new services with appropriate mounts and dependencies; only Grafana has a healthcheck; Promtail mounts `/var/lib/docker/containers:ro`; `docker compose config --quiet` validates; existing services are byte-identical.</done>
</task>

</tasks>

<verification>
- `docker compose config --quiet` exits 0 after Task 2.
- `docker compose up -d` (smoke-tested at the end of plan 02-03, not here) eventually reports Grafana as healthy and Loki/Promtail as running. Loki and Promtail intentionally have no healthcheck (Distroless images); Plan 02-03's smoke test probes them via curl from the host. Plan 02-01 alone does NOT bring the services up because the configs in `monitoring/` are still placeholders — that's intentional.
- Existing services (`postgres`, `dashboard`, etc.) are byte-identical in the diff to their pre-plan state.
</verification>

<success_criteria>
- LOGINFRA-01 partially satisfied: compose services for Loki / Promtail / Grafana declared with volumes and dependencies; Grafana has a healthcheck. Full satisfaction (services actually responding at startup) lands at the end of plan 02-03's smoke test, which probes Loki and Promtail via curl from the host.
- Mount paths `./monitoring/loki`, `./monitoring/promtail`, `./monitoring/grafana/provisioning`, `./monitoring/grafana/dashboards` exist as directories so plans 02-02 / 02-03 can write into them.
- Promtail has the `/var/lib/docker/containers:ro` mount so its `containers` scrape job (defined in plan 02-02) can tail container stdout JSON files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-log-infrastructure/02-01-SUMMARY.md` documenting:
- Exact image tags pulled (record from `docker compose pull loki promtail grafana` output if you ran it as part of verification)
- Healthcheck endpoints and the wget commands chosen (so plan 02-03's smoke test can mirror them)
- Confirmation that `docker compose config --quiet` validates
- Note whether `docker compose up -d loki promtail grafana` was attempted; if attempted, expected outcome is "loki healthy, promtail and grafana failing/restarting until configs land" — that's fine.
</output>
