---
phase: 02-log-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - monitoring/loki/loki-config.yml
  - monitoring/promtail/promtail-config.yml
autonomous: true
requirements: [LOGINFRA-02, LOGINFRA-03, LOGINFRA-04]

must_haves:
  truths:
    - "Loki runs in single-binary mode with TSDB schema, filesystem chunk storage, and 7-day retention enabled via the compactor"
    - "Promtail scrapes container stdout via `docker_sd_configs` reading `/var/run/docker.sock`"
    - "Promtail scrapes `/var/log/scraper.log` and `/var/log/scrapy_runs/spawn-*.log` via `static_configs`"
    - "Per-spawn log filename is parsed by a regex stage into `role` and `shop` labels"
    - "Log lines get a `level` label parsed by a regex pipeline stage from the log content"
    - "`run_id` is NEVER promoted to a Loki label — it must stay in log content (LogQL `|= \"run_id=N\"` filters it)"
  artifacts:
    - path: "monitoring/loki/loki-config.yml"
      provides: "Single-binary Loki config: TSDB schema, filesystem store, 168h retention via compactor"
      contains: |
        keys: auth_enabled, server, common, schema_config, storage_config, limits_config, compactor
    - path: "monitoring/promtail/promtail-config.yml"
      provides: "Two scrape jobs: docker_sd_configs for container stdout + static_configs for /var/log files"
      contains: |
        scrape_configs blocks named `containers` and `scrapy_spawn_files` and `scraper_log_file`
  key_links:
    - from: "monitoring/promtail/promtail-config.yml `scrapy_spawn_files` job"
      to: "spawn-YYYYMMDD-HHMMSSffffff-<role>-<shop>.log filename convention"
      via: "relabel_configs: regex `spawn-\\d{8}-\\d{14}-([^-]+)-(.+)\\.log` extracts role + shop"
      pattern: "spawn-.*-.*-.*\\.log"
    - from: "monitoring/promtail/promtail-config.yml clients[0]"
      to: "Loki push endpoint on the default compose network"
      via: "url: http://loki:3100/loki/api/v1/push"
      pattern: "http://loki:3100"
---

<objective>
Write the two config files that turn the empty Loki + Promtail containers (declared in plan 02-01) into a working log pipeline. Loki gets a single-binary TSDB config with 7d retention; Promtail gets three scrape jobs (Docker SD for container stdout, two `static_configs` jobs for files in `/var/log`) with regex relabeling to extract `service`, `level`, `role`, `shop` labels.

This plan is **independent of plan 02-01** in code terms — the files are wholly new — but both plans target the same wave because plan 02-01's bind mounts reference these files' paths. Either landing first is fine; the smoke test in plan 02-03 needs both.

Phase 3 will provision dashboards on top of Loki; this plan only stands up the substrate.
</objective>

<execution_context>
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-log-infrastructure/02-CONTEXT.md
@book_scraper/spawn_logging.py
@CLAUDE.md

<interfaces>
**Spawn-log filename convention** (from `book_scraper/spawn_logging.py:85-86`):

```
spawn-YYYYMMDD-HHMMSSffffff-<role>-<shop>.log
```

Where:
- `YYYYMMDD-HHMMSSffffff` is `datetime.strftime("%Y%m%d-%H%M%S%f")` — 8 digits, hyphen, 14 digits.
- `<role>` ∈ `operator | stall-resume | cron-chain | reconcile-restart` (plan 02 / phase 4 adds the fourth value)
- `<shop>` ∈ `vaga | pegasas | humanitas | <future shop>`

Both `<role>` and `<shop>` are slugged (lowercased, `[^a-z0-9-]` → `_`), so the regex character class needs `[a-z0-9_-]+`.

**Other in-container log files** (only one for now):
- `/var/log/scraper.log` — cron-output aggregate, label `service=scraper` and `role=cron`.

**Loki push endpoint**: `http://loki:3100/loki/api/v1/push` (DNS-resolvable on the default compose network).

**Docker socket**: Promtail mounts `/var/run/docker.sock:ro`. `docker_sd_configs` exposes labels like `__meta_docker_container_name`, `__meta_docker_container_label_com_docker_compose_service` — the latter is what we relabel to `service`.

**Cardinality contract from CONTEXT.md**:
| Label | Source | Bounded values |
|---|---|---|
| `service` | docker label `com.docker.compose.service` | dashboard / scraper / postgres / flaresolverr / loki / promtail / grafana |
| `level` | regex on log line | INFO / WARNING / ERROR / DEBUG / CRITICAL |
| `role` | filename | operator / stall-resume / cron-chain / reconcile-restart / cron |
| `shop` | filename | vaga / pegasas / humanitas / <future> |

`run_id` is forbidden as a label. Document this in a comment in the Promtail config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Loki single-binary config with TSDB schema and 7d retention</name>
  <files>monitoring/loki/loki-config.yml</files>
  <read_first>
    - .planning/phases/02-log-infrastructure/02-CONTEXT.md (retention decision: 7d explicit)
    - monitoring/loki/.gitkeep (confirm directory exists from plan 02-01 task 2)
  </read_first>
  <action>
Create `monitoring/loki/loki-config.yml` with these EXACT contents:

```yaml
# Loki single-binary config for the book-scraper observability stack.
#
# Design notes (see .planning/phases/02-log-infrastructure/02-CONTEXT.md):
# - Single-binary mode: filesystem chunk store, no S3/MinIO, no Memberlist.
# - TSDB schema (Loki 3.0+ default).
# - 7-day retention enabled via the compactor (LOGINFRA-04).
# - auth_enabled: false — single-tenant deployment on a private compose network.

auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: info

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

storage_config:
  tsdb_shipper:
    active_index_directory: /loki/tsdb-index
    cache_location: /loki/tsdb-cache
  filesystem:
    directory: /loki/chunks

limits_config:
  # 7-day retention satisfies LOGINFRA-04.
  retention_period: 168h
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  # Single-tenant defaults; keep modest to avoid surprise OOM on a personal machine.
  ingestion_rate_mb: 4
  ingestion_burst_size_mb: 6
  max_entries_limit_per_query: 5000

compactor:
  working_directory: /loki/compactor
  compaction_interval: 10m
  retention_enabled: true
  retention_delete_delay: 2h
  retention_delete_worker_count: 150
  delete_request_store: filesystem

# Ruler / alertmanager intentionally absent — alerting deferred per CONTEXT.md.
```

**Notes:**
- Indentation MUST be exactly 2 spaces (Loki uses strict YAML parsing).
- `replication_factor: 1` is required for single-binary mode.
- `retention_period: 168h` (7 days) is the LOGINFRA-04 contract. Do not change to a longer value here — extending retention is OBS-AUTO-02 future work and requires an object-storage backend.
- The compactor block is what *actually* deletes old chunks. Without `retention_enabled: true` and `compaction_interval`, Loki would write logs forever despite the `limits_config.retention_period`.
  </action>
  <verify>
    <automated>
[ -f monitoring/loki/loki-config.yml ] &&
grep -q "^auth_enabled: false" monitoring/loki/loki-config.yml &&
grep -q "retention_period: 168h" monitoring/loki/loki-config.yml &&
grep -q "retention_enabled: true" monitoring/loki/loki-config.yml &&
grep -q "store: tsdb" monitoring/loki/loki-config.yml &&
grep -q "schema: v13" monitoring/loki/loki-config.yml &&
python3 -c "import yaml; yaml.safe_load(open('monitoring/loki/loki-config.yml'))"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `monitoring/loki/loki-config.yml` exists
    - `grep -c "retention_period: 168h" monitoring/loki/loki-config.yml` returns 1
    - `grep -c "retention_enabled: true" monitoring/loki/loki-config.yml` returns 1
    - `grep -c "store: tsdb" monitoring/loki/loki-config.yml` returns 1
    - `grep -c "schema: v13" monitoring/loki/loki-config.yml` returns 1
    - `python3 -c "import yaml; print(yaml.safe_load(open('monitoring/loki/loki-config.yml')).get('auth_enabled'))"` prints `False`
    - When the loki container starts with this config mounted at `/etc/loki/loki-config.yml`, its `/ready` endpoint eventually responds with `ready` (verified in plan 02-03's smoke test)
  </acceptance_criteria>
  <done>Loki config file present, YAML-valid, all four contract values (retention 168h, retention_enabled true, TSDB store, schema v13) present.</done>
</task>

<task type="auto">
  <name>Task 2: Write Promtail config with three scrape jobs and label-cardinality guardrails</name>
  <files>monitoring/promtail/promtail-config.yml</files>
  <read_first>
    - book_scraper/spawn_logging.py (filename convention `spawn-YYYYMMDD-HHMMSSffffff-<role>-<shop>.log` — confirm the regex in this task matches)
    - .planning/phases/02-log-infrastructure/02-CONTEXT.md (cardinality contract: only service / level / role / shop)
    - monitoring/promtail/.gitkeep (confirm directory exists)
  </read_first>
  <action>
Create `monitoring/promtail/promtail-config.yml` with these EXACT contents:

```yaml
# Promtail config for the book-scraper observability stack.
#
# Three scrape jobs:
#   1. containers       — container stdout via docker_sd_configs
#   2. scrapy_spawn_files — /var/log/scrapy_runs/spawn-*.log via static_configs
#   3. scraper_log_file  — /var/log/scraper.log (cron-output aggregate)
#
# LABEL CARDINALITY CONTRACT (.planning/phases/02-log-infrastructure/02-CONTEXT.md):
#   Allowed labels:  service, level, role, shop
#   Forbidden:       run_id (would explode the index — use `|= "run_id=N"` in LogQL instead)
#                    container_id, docker_image (low filter value, high cardinality)

server:
  http_listen_port: 9080
  grpc_listen_port: 0
  log_level: info

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:

  # ─── 1. Container stdout (Docker service discovery) ───────────────────
  - job_name: containers
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 15s
    relabel_configs:
      # Use the compose service name as `service` label.
      - source_labels: ['__meta_docker_container_label_com_docker_compose_service']
        target_label: service
      # Drop containers without a compose service label (e.g., one-off `docker run`).
      - source_labels: [service]
        regex: '^$'
        action: drop
      # Promtail needs `__path__` to know where to read; docker_sd doesn't set it,
      # so resolve via the standard /var/lib/docker/containers/<id>/<id>-json.log path
      # that Docker writes by default. Source the container id from the meta label.
      - source_labels: ['__meta_docker_container_id']
        target_label: '__path__'
        replacement: '/var/lib/docker/containers/$1/$1-json.log'
    pipeline_stages:
      # Docker writes JSON-per-line; extract the actual log line.
      - json:
          expressions:
            output: log
            stream: stream
            timestamp: time
      - timestamp:
          source: timestamp
          format: RFC3339Nano
      - output:
          source: output
      # Parse level from common Python / uvicorn / scrapy formats.
      # Examples we hit: "INFO  [alembic.runtime.migration] ..."
      #                  "2026-05-12 16:58:58 [scrapy.core.engine] INFO: Spider closed"
      #                  "ERROR:    Application startup failed"
      - regex:
          expression: '(?P<level>DEBUG|INFO|WARNING|WARN|ERROR|CRITICAL|FATAL)\b'
      - labels:
          level:

  # ─── 2. Per-spawn scrapy log files ────────────────────────────────────
  # Filenames: spawn-YYYYMMDD-HHMMSSffffff-<role>-<shop>.log
  #   <role>: operator | stall-resume | cron-chain | reconcile-restart
  #   <shop>: vaga | pegasas | humanitas | ...
  - job_name: scrapy_spawn_files
    static_configs:
      - targets:
          - localhost
        labels:
          service: scraper
          job: scrapy_spawn_files
          __path__: /var/log/scrapy_runs/spawn-*.log
    pipeline_stages:
      # Extract role + shop from the filename. The regex anchors are loose so a
      # role-name containing hyphens (e.g. `stall-resume`) still parses correctly:
      # the timestamp portion always ends with exactly 14 digits.
      - regex:
          source: filename
          expression: '/spawn-\d{8}-\d{14}-(?P<role>[a-z0-9_-]+?)-(?P<shop>[a-z0-9_-]+)\.log$'
      - labels:
          role:
          shop:
      - regex:
          expression: '(?P<level>DEBUG|INFO|WARNING|WARN|ERROR|CRITICAL|FATAL)\b'
      - labels:
          level:

  # ─── 3. Cron-output aggregate file ────────────────────────────────────
  - job_name: scraper_log_file
    static_configs:
      - targets:
          - localhost
        labels:
          service: scraper
          role: cron
          job: scraper_log_file
          __path__: /var/log/scraper.log
    pipeline_stages:
      - regex:
          expression: '(?P<level>DEBUG|INFO|WARNING|WARN|ERROR|CRITICAL|FATAL)\b'
      - labels:
          level:
```

**Notes:**
- The `docker_sd_configs` job reads via the Docker socket Promtail has mounted at `/var/run/docker.sock:ro`. It auto-discovers every container in the compose project.
- The `__path__` rewrite to `/var/lib/docker/containers/$1/$1-json.log` is the canonical Promtail pattern for Docker SD. Promtail needs a host file to tail; Docker writes JSON-formatted logs to that path by default. **This path is inside the host filesystem, not the container** — for it to work on the macOS Docker Desktop VM, Promtail needs read access to that path. (Plan 02-03's smoke test will confirm; if it fails, the fallback is the `loki` Docker logging driver — listed as a deferred mitigation.)
- The role regex uses `?` (non-greedy) so `stall-resume-vaga` splits correctly into `role=stall-resume`, `shop=vaga` rather than `role=stall`, `shop=resume-vaga`. The trailing `-(?P<shop>[a-z0-9_-]+)` greedy match anchors the boundary at the LAST hyphen, which combined with the non-greedy role gives correct splitting.
- `run_id` is **deliberately not extracted** even though log lines will contain it. The comment block at the top of the file documents why.
- The `level` regex is run on the OUTPUT of the json-parse stage in job 1, and on the raw line in jobs 2 and 3. It's intentionally tolerant of multiple log formats.
  </action>
  <verify>
    <automated>
[ -f monitoring/promtail/promtail-config.yml ] &&
grep -q "url: http://loki:3100/loki/api/v1/push" monitoring/promtail/promtail-config.yml &&
grep -q "job_name: containers" monitoring/promtail/promtail-config.yml &&
grep -q "job_name: scrapy_spawn_files" monitoring/promtail/promtail-config.yml &&
grep -q "job_name: scraper_log_file" monitoring/promtail/promtail-config.yml &&
grep -q "docker_sd_configs:" monitoring/promtail/promtail-config.yml &&
grep -q "/var/log/scrapy_runs/spawn-\*\.log" monitoring/promtail/promtail-config.yml &&
grep -q "/var/log/scraper.log" monitoring/promtail/promtail-config.yml &&
! grep -q "run_id" monitoring/promtail/promtail-config.yml | grep -v "^#" &&
python3 -c "import yaml; cfg = yaml.safe_load(open('monitoring/promtail/promtail-config.yml')); assert len(cfg['scrape_configs']) == 3"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `monitoring/promtail/promtail-config.yml` exists
    - `python3 -c "import yaml; yaml.safe_load(open('monitoring/promtail/promtail-config.yml'))"` exits 0 (YAML-valid)
    - `grep -c "job_name:" monitoring/promtail/promtail-config.yml` returns exactly 3
    - `grep -c "docker_sd_configs:" monitoring/promtail/promtail-config.yml` returns 1
    - `grep -c "url: http://loki:3100/loki/api/v1/push" monitoring/promtail/promtail-config.yml` returns 1
    - `grep -c "FORBIDDEN" monitoring/promtail/promtail-config.yml` (or `forbidden`, case-insensitive) returns ≥1 — the run_id guardrail comment is present
    - `grep -E "^[^#]*run_id" monitoring/promtail/promtail-config.yml` returns nothing (no non-comment mention)
    - `grep -c "spawn-" monitoring/promtail/promtail-config.yml` returns ≥1 (filename pattern referenced)
    - When Promtail loads this config with the `monitoring/promtail` dir mounted, its `/ready` endpoint responds with `Ready` (verified in plan 02-03 smoke test)
  </acceptance_criteria>
  <done>Promtail config file present, YAML-valid, three scrape jobs declared, Loki push URL points at `loki:3100`, run_id guardrail comment included.</done>
</task>

</tasks>

<verification>
- Both files load via `python3 -c "import yaml; yaml.safe_load(open(<path>))"` without exception.
- The label-cardinality contract from CONTEXT.md is honored — only `service`, `level`, `role`, `shop` appear as `labels:` directives in Promtail (and `job` which is Promtail's own meta-label, always low-cardinality).
- Smoke test of "Promtail actually reaches Loki" is deferred to plan 02-03 — that's where we boot the stack and run `curl` against Loki's query endpoint.
</verification>

<success_criteria>
- LOGINFRA-02 satisfied: Promtail's `containers` scrape job tags lines with `service` from the compose service name (auto-discovers all containers via Docker SD).
- LOGINFRA-03 satisfied: Promtail's `scrapy_spawn_files` job parses `role` + `shop` from the spawn-log filename; `scraper_log_file` job tags `/var/log/scraper.log` with `service=scraper`, `role=cron`.
- LOGINFRA-04 satisfied: Loki config has `retention_period: 168h` + `retention_enabled: true` in the compactor block, so chunks older than 7 days are deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/02-log-infrastructure/02-02-SUMMARY.md` documenting:
- Confirmation both YAML files validate via PyYAML
- The regex tested against three sample filenames: `spawn-20260512-165858302771-operator-vaga.log`, `spawn-20260513-100000000000-stall-resume-humanitas.log`, `spawn-20260513-100000000000-cron-chain-pegasas.log` — paste actual regex output showing role + shop extraction is correct
- Note any deviation from the planned config (Loki schema version, Promtail Docker SD path, etc.)
</output>
