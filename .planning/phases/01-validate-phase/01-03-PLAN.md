---
phase: 01-validate-phase
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - book_scraper/services/validate.py
  - tests/integration/test_validate_service.py
autonomous: true
requirements: [VAL-05, VAL-06, VAL-07, VAL-08, VAL-09, VAL-10, VAL-11]

must_haves:
  truths:
    - "ValidateService.run() executes ALL seven check groups (structural, slug-title, completeness, correctness, classification, staleness, match-readiness, relationship integrity) and returns counters for each issue key encountered"
    - "Re-running validate on identical data does not create duplicate ValidationIssue rows — second run flips lifecycle_state to `recurring` (or equivalent state recorded by `_assign_lifecycle_states`)"
    - "stale_active uses `last_seen_at < now() - 2 * VALIDATE_STALE_CADENCE_DAYS` — defaults to 28 days"
    - "year_out_of_range fires for year < 1800 or year > current_year + 2"
    - "format_is_dimensions matches `^\\d+.*[xX×].*\\d+`"
    - "non_product_active joins ShopBook to DiscoveredUrl and flags rows where url_type='non_product' AND is_active=true"
    - "url_aliases flags any shop_book_id with COUNT(discovered_urls) > 1"
    - "match_isbn_drift joins to `books` and flags rows where match_status='matched' AND books.isbn != shop_books.isbn (both non-null)"
  artifacts:
    - path: "book_scraper/services/validate.py"
      provides: "Extended ValidateService with all 19 issue keys across 7 check groups"
      contains: "check_data_completeness"
    - path: "tests/integration/test_validate_service.py"
      provides: "End-to-end tests against real PostgreSQL covering each check group + dedup invariant"
      contains: "test_dedup"
  key_links:
    - from: "tests/integration/test_validate_service.py"
      to: "book_scraper/services/validate.py"
      via: "from book_scraper.services.validate import ValidateService, VALIDATE_STALE_CADENCE_DAYS"
      pattern: "from book_scraper\\.services\\.validate import"
    - from: "book_scraper/services/validate.py"
      to: "book_scraper/db/models.py (DiscoveredUrl)"
      via: "JOIN shop_books -> discovered_urls for non_product_active, unreachable_active, orphan_no_url, url_aliases, product_url_non_book"
      pattern: "discovered_urls"
---

<objective>
Extend ValidateService with the remaining five check groups defined in the design spec, then add integration tests against real PostgreSQL covering all 19 issue keys plus the deduplication invariant. After this plan, every check listed in the spec table is implemented and writes correct ValidationIssue rows for the synthetic fixture data.

Purpose: Plan 02 established the spider + service shell with two check groups. This plan completes the check coverage so the phase satisfies VAL-05 through VAL-10 and proves VAL-11 holds end-to-end (not just by delegation to `_assign_lifecycle_states`).

Output: Extended `book_scraper/services/validate.py` (only the ValidateService class is touched — module imports may grow) and a new integration test file. All tests green via `uv run pytest tests/integration/test_validate_service.py -v`.
</objective>

<execution_context>
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-validate-phase/01-CONTEXT.md
@.planning/phases/01-validate-phase/01-RESEARCH.md
@CLAUDE.md
@docs/superpowers/specs/2026-05-10-shop-books-validate-phase-design.md
@.planning/phases/01-validate-phase/01-02-SUMMARY.md
@book_scraper/services/validate.py

<interfaces>
<!-- DB tables and models exercised by the new check groups. All verified. -->

`shop_books` columns relied on (from book_scraper/db/models.py:89-200):
- id, shop_id, url, title, author, sku, isbn, year, format, type
- is_active, in_stock, last_seen_at, match_status

`discovered_urls` columns (from book_scraper/db/models.py:375-410):
- id, shop_id, url, normalized_url, url_type, last_seen_at
- url_type values include: 'product', 'category', 'unreachable', 'non_product', 'product_partial', ...
- ShopBook does NOT FK directly to DiscoveredUrl; the link is via matching shop_id + url
  (the url-aliases check uses normalized_url; see existing query patterns in repo.py).

`prices` table (from book_scraper/db/models.py — search for `class Price`):
- shop_book_id, price, currency, scraped_at
- LEFT JOIN to detect `no_price_history`.

`books` table (canonical, from book_scraper/db/models.py:822+):
- id, isbn (NOT NULL, unique)
- ShopBook.match_status='matched' implies a join via... CHECK the actual join column. The
  research file says "JOIN to `books` table for `match_isbn_drift`". Inspect models.py
  lines 800-900 to find the FK or join condition (likely `shop_books.matched_book_id` or
  similar). Read the file before writing the SQL.

From plan 02:
```python
# book_scraper/services/validate.py — module exports already in place
VALIDATE_STALE_CADENCE_DAYS: int = 14

class ValidateService:
    def __init__(self, session: Session) -> None: ...
    def run(self, shop_id: int, run_id: int) -> dict[str, int]: ...
    def check_structural_duplicates(self, shop_id: int, run_id: int) -> list[dict]: ...
    def check_slug_title_mismatch(self, shop_id: int, run_id: int) -> list[dict]: ...

def _tokenize(s: str) -> set[str]: ...
def _should_flag_slug_title(slug: str | None, title: str | None) -> bool: ...
```

Existing integration test scaffolding to mirror:
- `tests/integration/test_repo_validation_issues.py` if it exists, else `tests/integration/test_match_service.py` for the session/fixture pattern.
- Tests use the real Postgres on port 5433 (test DB). The conftest.py provides a `session` fixture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ValidateService with completeness, correctness, and classification checks</name>
  <files>book_scraper/services/validate.py</files>
  <read_first>
    - book_scraper/services/validate.py (current state from plan 02 — extend, don't rewrite)
    - book_scraper/db/models.py lines 89-200 (ShopBook full field list including type, is_active, in_stock)
    - book_scraper/db/models.py lines 375-420 (DiscoveredUrl columns + url_type values)
    - book_scraper/db/models.py — find `class Price` (used for no_price_history LEFT JOIN)
    - docs/superpowers/specs/2026-05-10-shop-books-validate-phase-design.md (sections "Data completeness", "Data correctness", "Classification consistency")
  </read_first>
  <behavior>
    Add three methods to `ValidateService`. Each returns `list[dict]` and is called from `run()`.

    **`check_data_completeness(shop_id, run_id) -> list[dict]`** — VAL-05:
    - `active_no_price`: rows with `is_active = true AND price IS NULL`. field='price', issue='active_no_price', raw_value=NULL.
    - `in_stock_no_price`: rows with `is_active = true AND in_stock = true AND price IS NULL`. field='price', issue='in_stock_no_price'.
    - `book_no_metadata`: rows with `type = 'book' AND isbn IS NULL AND author IS NULL AND year IS NULL`. field='metadata', issue='book_no_metadata'.
    - `no_price_history`: rows with `is_active = true` and NO matching prices.shop_book_id. Use LEFT JOIN ... WHERE prices.id IS NULL. field='price_history', issue='no_price_history'.
    - NOTE: shop_books does not have its own `price` column on the spec? Actually it does (verified via dashboard queries — search for `sb.price`). Confirm by reading models.py during implementation. If `price` is NOT on shop_books directly, derive it from the latest price row. Default assumption: shop_books.price exists; if not, use `EXISTS (SELECT 1 FROM prices p WHERE p.shop_book_id = sb.id)`.

    **`check_data_correctness(shop_id, run_id) -> list[dict]`** — VAL-06:
    - `year_out_of_range`: `year IS NOT NULL AND (year < 1800 OR year > extract(year from now())::int + 2)`. field='year', issue='year_out_of_range', raw_value=str(year).
    - `price_zero`: `price = 0` (or, if shop_books has no price column, latest `prices.price = 0`). field='price', issue='price_zero', raw_value='0'.
    - `format_is_dimensions`: `format ~ '^\d+.*[xX×].*\d+'` using PostgreSQL regex operator `~`. field='format', issue='format_is_dimensions', raw_value=row.format.

    **`check_classification_consistency(shop_id, run_id) -> list[dict]`** — VAL-07:
    - `book_no_signals`: `type = 'book' AND isbn IS NULL AND author IS NULL AND year IS NULL AND format IS NULL`. field='type', issue='book_no_signals'.
    - `non_book_has_isbn`: `type = 'non_book' AND isbn IS NOT NULL`. field='type', issue='non_book_has_isbn', raw_value=row.isbn.
    - `non_product_active`: JOIN on shop_books.url = discovered_urls.url (within same shop_id) WHERE `discovered_urls.url_type = 'non_product' AND shop_books.is_active = true`. field='url_type', issue='non_product_active', raw_value='non_product'.

    Update `run()` to call all three new methods, extend the issue list, count issue keys.

    Acceptance behaviour: ruff + mypy clean, no `session.commit()` introduced inside the service.
  </behavior>
  <action>
Edit `book_scraper/services/validate.py` and append the three new methods to `ValidateService`. Update `run()`:

```python
def run(self, shop_id: int, run_id: int) -> dict[str, int]:
    issues: list[dict] = []
    issues.extend(self.check_structural_duplicates(shop_id, run_id))
    issues.extend(self.check_slug_title_mismatch(shop_id, run_id))
    issues.extend(self.check_data_completeness(shop_id, run_id))
    issues.extend(self.check_data_correctness(shop_id, run_id))
    issues.extend(self.check_classification_consistency(shop_id, run_id))
    # Plan 03 Task 2 will append three more calls here.
    if issues:
        bulk_insert_validation_issues(self._session, issues, shop_id=shop_id)
    counters: dict[str, int] = {}
    for it in issues:
        key = str(it["issue"])
        counters[key] = counters.get(key, 0) + 1
    return counters
```

**Implementation guidelines for each new method:**
- Use `text(...)` SQL queries for readability and to keep parity with `check_structural_duplicates`.
- Always `SELECT sb.id, sb.url, ...whatever-is-needed` so the dict can populate `shop_book_id` directly (skips the URL-resolution path in `bulk_insert_validation_issues`).
- The dict shape is constant: `{"scrape_run_id": run_id, "url": row.url, "field": "<field>", "issue": "<issue_key>", "raw_value": <value-or-None>, "shop_book_id": row.id}`.
- For `non_product_active`, the JOIN should be on `discovered_urls.shop_id = sb.shop_id AND discovered_urls.url = sb.url` (NOT on normalized_url, because shop_books.url is already canonical at write time per `upsert_shop_book`). Confirm this by reading `book_scraper/db/repo.py` `upsert_shop_book` if uncertain.
- For `no_price_history`: `LEFT JOIN prices p ON p.shop_book_id = sb.id WHERE sb.shop_id = :shop_id AND sb.is_active = true AND p.id IS NULL`. Use a subquery if needed for performance, but a LEFT JOIN with `IS NULL` is the canonical idiom.

Do NOT add severity annotations — the spec assigns severity by issue key but ValidationIssue has no severity column; dashboard rendering picks severity from the issue key.

Do NOT modify `check_structural_duplicates` or `check_slug_title_mismatch` — they are locked from plan 02.

Run formatters at the end:
```
uv run ruff format book_scraper/services/validate.py
uv run ruff check book_scraper/services/validate.py
uv run mypy book_scraper/services/validate.py
```
  </action>
  <verify>
    <automated>
uv run ruff check book_scraper/services/validate.py &&
uv run mypy book_scraper/services/validate.py &&
uv run pytest tests/unit/test_validate_service_structural.py tests/unit/test_validate_spider.py -v
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def check_data_completeness" book_scraper/services/validate.py` returns 1
    - `grep -c "def check_data_correctness" book_scraper/services/validate.py` returns 1
    - `grep -c "def check_classification_consistency" book_scraper/services/validate.py` returns 1
    - `grep -c "active_no_price" book_scraper/services/validate.py` >= 1
    - `grep -c "in_stock_no_price" book_scraper/services/validate.py` >= 1
    - `grep -c "book_no_metadata" book_scraper/services/validate.py` >= 1
    - `grep -c "no_price_history" book_scraper/services/validate.py` >= 1
    - `grep -c "year_out_of_range" book_scraper/services/validate.py` >= 1
    - `grep -c "price_zero" book_scraper/services/validate.py` >= 1
    - `grep -c "format_is_dimensions" book_scraper/services/validate.py` >= 1
    - `grep -c "book_no_signals" book_scraper/services/validate.py` >= 1
    - `grep -c "non_book_has_isbn" book_scraper/services/validate.py` >= 1
    - `grep -c "non_product_active" book_scraper/services/validate.py` >= 1
    - `grep -v '^#' book_scraper/services/validate.py | grep -c "session.commit"` returns 0
    - Plan 02's unit tests still pass (regression guard).
    - mypy + ruff exit 0.
  </acceptance_criteria>
  <done>Three new check methods present in ValidateService, run() invokes them, all 12 issue keys covered so far appear as string literals in the file, prior unit tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ValidateService with staleness, match-readiness, relationship integrity checks + integration tests</name>
  <files>
    book_scraper/services/validate.py
    tests/integration/test_validate_service.py
  </files>
  <read_first>
    - book_scraper/services/validate.py (state after Task 1)
    - book_scraper/db/models.py lines 800-920 (Book model, the join column from ShopBook to Book)
    - book_scraper/db/models.py lines 375-420 (DiscoveredUrl)
    - tests/integration/test_match_service.py (fixture / session pattern to clone)
    - tests/integration/conftest.py (find the `session` fixture wired to the test DB on port 5433)
    - book_scraper/db/repo.py lines 1500-1660 (verify `_assign_lifecycle_states` behaviour: what does the second call to `bulk_insert_validation_issues` produce when the same issue exists already?)
    - docs/superpowers/specs/2026-05-10-shop-books-validate-phase-design.md (sections "Staleness/lifecycle", "Match phase readiness", "Relationship integrity")
  </read_first>
  <behavior>
    Add three more methods to `ValidateService`:

    **`check_staleness(shop_id, run_id) -> list[dict]`** — VAL-08:
    - `stale_active`: `is_active = true AND last_seen_at < now() - INTERVAL ':days days'` where `:days = 2 * VALIDATE_STALE_CADENCE_DAYS`. Use `text(...)` with bound param. field='last_seen_at', issue='stale_active', raw_value=last_seen_at ISO string.
    - `unreachable_active`: JOIN to discovered_urls WHERE `discovered_urls.url_type = 'unreachable' AND shop_books.is_active = true`. field='url_type', issue='unreachable_active', raw_value='unreachable'.
    - `orphan_no_url`: shop_books rows with NO matching discovered_urls row (LEFT JOIN ... IS NULL on shop_id + url). field='url', issue='orphan_no_url', raw_value=row.url.

    **`check_match_readiness(shop_id, run_id) -> list[dict]`** — VAL-09:
    - `unmatched_has_isbn`: `match_status = 'unmatched' AND isbn IS NOT NULL`. field='match_status', issue='unmatched_has_isbn', raw_value=row.isbn.
    - `match_isbn_drift`: JOIN to books table on the matched-book FK column (read models.py to find it — likely `shop_books.matched_book_id` or similar). Filter `match_status = 'matched' AND books.isbn IS NOT NULL AND shop_books.isbn IS NOT NULL AND books.isbn != shop_books.isbn`. field='isbn', issue='match_isbn_drift', raw_value=f"{shop_books.isbn} vs {books.isbn}".

    **`check_relationship_integrity(shop_id, run_id) -> list[dict]`** — VAL-10:
    - `url_aliases`: GROUP BY shop_book matching key — find shop_books with > 1 matching discovered_urls (same shop_id + url match, considering normalized_url). Approach: COUNT(DISTINCT discovered_urls.id) > 1 grouped by shop_book_id. field='url', issue='url_aliases', raw_value=str(count).
    - `product_url_non_book`: JOIN discovered_urls (url_type='product') to shop_books (type='non_book'). field='type', issue='product_url_non_book', raw_value='non_book'.

    Update `run()` to extend with the three new method calls.

    **Integration tests** in `tests/integration/test_validate_service.py` (use the `session` fixture from conftest.py — real Postgres on port 5433):

    Use one helper `_make_shop(session)` returning `Shop` row, then per-test helpers to insert ShopBook + DiscoveredUrl rows directly.

    Required test functions (each MUST be present):
    1. `test_isbn_duplicate_flags_both_rows`: insert two shop_books with same ISBN. Run ValidateService. Assert exactly 2 ValidationIssue rows with issue='isbn_duplicate', one per shop_book.
    2. `test_title_author_duplicate_flags_both_rows`: same pattern but identical title+author, different ISBN.
    3. `test_sku_duplicate_flags_both_rows`: same pattern with SKU.
    4. `test_slug_title_mismatch_zero_overlap_flagged`: shop_book with url ending in `/vyresnio-amziaus-zmoniu-sveika`, title `Ką šunys galvoja?`. Assert one slug_title_mismatch row.
    5. `test_slug_title_mismatch_with_overlap_not_flagged`: url ending in `/sapiens-trumpa-zmonijos-istorija`, title `Sapiens trumpa istorija`. Assert NO slug_title_mismatch row for this shop_book.
    6. `test_active_no_price_flagged_when_price_null`: shop_book is_active=true, price=NULL. Assert active_no_price issue present (and in_stock_no_price if in_stock=true).
    7. `test_year_out_of_range_flagged`: shop_book year=1700 → flagged; year=current_year+5 → flagged; year=2024 → not flagged.
    8. `test_format_is_dimensions_flagged`: shop_book format='15x20x3 cm' → flagged; format='hardcover' → not flagged.
    9. `test_non_product_active_flagged_via_join`: discovered_urls row with url_type='non_product', matching shop_book is_active=true → flagged.
    10. `test_stale_active_flagged_when_last_seen_old`: shop_book is_active=true, last_seen_at = now() - 30 days → flagged (28-day threshold = 2 * 14). Verify against `VALIDATE_STALE_CADENCE_DAYS` import.
    11. `test_orphan_no_url_flagged`: shop_book with no matching discovered_urls → flagged.
    12. `test_unmatched_has_isbn_flagged`: shop_book match_status='unmatched', isbn='9789999999999' → flagged.
    13. `test_url_aliases_flagged_when_multiple_urls_per_shop_book`: insert one shop_book + two discovered_urls referencing it → flagged.
    14. `test_dedup_second_run_does_not_create_duplicate_rows`: insert one duplicate-ISBN pair → run validate → assert 2 rows. Run validate AGAIN against the same session → assert still 2 rows total in `validation_issues` for that pair (not 4). Inspect `lifecycle_state` on the rows from the second run — accept any non-`new` state (the actual transition value depends on `_assign_lifecycle_states` — could be `recurring`, `already_seen`, etc. — the requirement is "no new row is inserted," not a specific state name).
    15. `test_run_returns_counters_keyed_by_issue`: assert `counters["isbn_duplicate"] == 2` after the duplicate-ISBN scenario.

    For dedup test (#14), the spec wording is "deduplicates by (shop_book_id, field)"; but `_assign_lifecycle_states` actually keys on `(shop_book_id, field, issue)`. That's a superset (each check produces one issue per field), so the requirement holds. The test should assert: `SELECT COUNT(*) FROM validation_issues WHERE shop_book_id IN (...) AND field = 'isbn'` returns 2 after both runs.

    Tests use `session.commit()` after `ValidateService.run()` (matching spider behaviour).

    Skip integration tests gracefully if test DB is not reachable: use the standard project pattern from `tests/integration/test_match_service.py` (session fixture handles the skip).
  </behavior>
  <action>
**Step 1 — Extend ValidateService:**

Append the three new methods to `book_scraper/services/validate.py` (after `check_classification_consistency`). Each follows the same dict-shape pattern. Update `run()` to call them too:

```python
def run(self, shop_id: int, run_id: int) -> dict[str, int]:
    issues: list[dict] = []
    issues.extend(self.check_structural_duplicates(shop_id, run_id))
    issues.extend(self.check_slug_title_mismatch(shop_id, run_id))
    issues.extend(self.check_data_completeness(shop_id, run_id))
    issues.extend(self.check_data_correctness(shop_id, run_id))
    issues.extend(self.check_classification_consistency(shop_id, run_id))
    issues.extend(self.check_staleness(shop_id, run_id))
    issues.extend(self.check_match_readiness(shop_id, run_id))
    issues.extend(self.check_relationship_integrity(shop_id, run_id))
    if issues:
        bulk_insert_validation_issues(self._session, issues, shop_id=shop_id)
    counters: dict[str, int] = {}
    for it in issues:
        key = str(it["issue"])
        counters[key] = counters.get(key, 0) + 1
    return counters
```

For `check_staleness` use a parameterised interval:
```python
days = 2 * VALIDATE_STALE_CADENCE_DAYS
rows = self._session.execute(
    text("SELECT id, url, last_seen_at FROM shop_books "
         "WHERE shop_id = :shop_id AND is_active = true "
         "AND last_seen_at < now() - make_interval(days => :days)"),
    {"shop_id": shop_id, "days": days},
).all()
```

For `check_match_readiness` you MUST first read `book_scraper/db/models.py` to find the actual join column from ShopBook to Book. Common candidates: `shop_books.matched_book_id`, `shop_books.book_id`, or a separate join table. Use whichever is correct; fail fast if neither exists (raise NotImplementedError with a clear message and create a follow-up issue — but the research file claims the join is straightforward, so the column should be discoverable).

For `check_relationship_integrity` `url_aliases`: the simplest approach is to count discovered_urls per (shop_id, url-key). Since shop_books.url is canonical, group discovered_urls by `normalized_url` per shop_id and join to shop_books on the canonical url. Implementation hint:
```sql
WITH counts AS (
  SELECT shop_id, normalized_url, COUNT(*) AS n
  FROM discovered_urls
  WHERE shop_id = :shop_id
  GROUP BY shop_id, normalized_url
  HAVING COUNT(*) > 1
)
SELECT sb.id, sb.url, c.n
FROM shop_books sb
JOIN counts c ON c.shop_id = sb.shop_id
              AND c.normalized_url = lower(sb.url)  -- adapt to actual normalize_url logic
WHERE sb.shop_id = :shop_id
```
If `normalized_url` mapping for shop_books.url is non-trivial, simplify by grouping discovered_urls by shop_book_id IF discovered_urls has a shop_book_id FK. Read the model file before writing the query. The semantics required by the spec are: "Multiple URLs → same shop_book → flag".

**Step 2 — Create integration tests:**

Create `tests/integration/test_validate_service.py`. Use the `session` fixture from conftest. Each test:
1. Inserts a Shop via `upsert_shop` (or directly).
2. Inserts ShopBook + DiscoveredUrl rows via direct ORM construction.
3. Creates a ScrapeRun row via `create_scrape_run(session, shop.id, "validate", ...)` (this is why this plan depends on plan 01: enum value must exist on the test DB).
4. `session.commit()`.
5. Calls `ValidateService(session).run(shop.id, run.id)`.
6. `session.commit()`.
7. Queries `ValidationIssue` rows and asserts the expected issue keys + counts.

The dedup test (#14) calls `ValidateService.run` twice with the same shop_id + run_id (or two different run_ids — both must satisfy "no new row" per VAL-11; verify which is correct by reading `_assign_lifecycle_states`). The spec explicitly says "Re-running validate on the same data does not create duplicate rows" — verify the assertion matches whichever run-id strategy mirrors production (the spider creates a fresh `run_id` per invocation, so the test should also use a fresh run_id on the second call).

Make sure the test DB has the `'validate'` enum value before tests run. The standard test setup runs `alembic upgrade head` on the test DB on init — verify by checking conftest.py. If it does NOT auto-migrate, add a session-scoped fixture that ensures `alembic upgrade head` was run against `book_scraper_test`.
  </action>
  <verify>
    <automated>
docker compose up -d postgres-test &&
PYTHONPATH=. DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/book_scraper_test uv run alembic upgrade head &&
uv run ruff check book_scraper/services/validate.py tests/integration/test_validate_service.py &&
uv run mypy book_scraper/services/validate.py &&
uv run pytest tests/integration/test_validate_service.py -v &&
uv run pytest tests/unit/test_validate_service_structural.py tests/unit/test_validate_spider.py -v
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def check_staleness" book_scraper/services/validate.py` returns 1
    - `grep -c "def check_match_readiness" book_scraper/services/validate.py` returns 1
    - `grep -c "def check_relationship_integrity" book_scraper/services/validate.py` returns 1
    - `grep -c "stale_active" book_scraper/services/validate.py` >= 1
    - `grep -c "unreachable_active" book_scraper/services/validate.py` >= 1
    - `grep -c "orphan_no_url" book_scraper/services/validate.py` >= 1
    - `grep -c "unmatched_has_isbn" book_scraper/services/validate.py` >= 1
    - `grep -c "match_isbn_drift" book_scraper/services/validate.py` >= 1
    - `grep -c "url_aliases" book_scraper/services/validate.py` >= 1
    - `grep -c "product_url_non_book" book_scraper/services/validate.py` >= 1
    - `grep -c "VALIDATE_STALE_CADENCE_DAYS" book_scraper/services/validate.py` >= 2 (definition + use in check_staleness)
    - File `tests/integration/test_validate_service.py` exists
    - `grep -c "^def test_" tests/integration/test_validate_service.py` >= 14 (one per test function listed in <behavior>)
    - `grep -c "test_dedup_second_run_does_not_create_duplicate_rows" tests/integration/test_validate_service.py` returns 1
    - `uv run pytest tests/integration/test_validate_service.py -v` reports >= 14 passing tests, 0 failures
    - All Plan 02 unit tests still pass (regression guard)
    - `uv run mypy book_scraper/services/validate.py` exits 0
    - `uv run ruff check book_scraper/services/validate.py tests/integration/test_validate_service.py` exits 0
  </acceptance_criteria>
  <done>All 7 check groups (19 issue keys) implemented in ValidateService, integration tests cover each issue key plus the dedup invariant, all unit + integration tests green.</done>
</task>

</tasks>

<verification>
- Full test sweep: `uv run pytest tests/unit/test_validate_service_structural.py tests/unit/test_validate_spider.py tests/integration/test_validate_service.py -v` is green.
- Smoke run end-to-end: `docker compose exec scraper /app/.venv/bin/scrapy crawl validate -a shop=vaga` (after rebuild from plan 04 deploy) creates a `phase='validate'` row and writes ValidationIssue rows. (This is verified at phase verification, not during this plan.)
- ruff + mypy clean across all modified files.
</verification>

<success_criteria>
- VAL-05 satisfied: completeness checks (active_no_price, in_stock_no_price, book_no_metadata, no_price_history) implemented + tested.
- VAL-06 satisfied: correctness checks (year_out_of_range, price_zero, format_is_dimensions) implemented + tested.
- VAL-07 satisfied: classification checks (book_no_signals, non_book_has_isbn, non_product_active) implemented + tested.
- VAL-08 satisfied: staleness checks (stale_active, unreachable_active, orphan_no_url) implemented + tested.
- VAL-09 satisfied: match-readiness checks (unmatched_has_isbn, match_isbn_drift) implemented + tested.
- VAL-10 satisfied: relationship integrity checks (url_aliases, product_url_non_book) implemented + tested.
- VAL-11 satisfied (proven end-to-end): dedup test demonstrates re-running validate on identical data does not create duplicate rows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-validate-phase/01-03-SUMMARY.md` documenting:
- Final list of methods on ValidateService (8 entries: 7 check groups + run)
- All 19 issue keys present in the file (provide grep-extracted list)
- Integration test count + pass count
- Any deviation from the spec (e.g. simplified url_aliases query) with rationale
- Whether the test DB required manual `alembic upgrade head` or whether conftest does it automatically
</output>
