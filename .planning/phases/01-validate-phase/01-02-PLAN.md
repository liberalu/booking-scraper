---
phase: 01-validate-phase
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - book_scraper/services/validate.py
  - book_scraper/spiders/validate.py
  - tests/unit/test_validate_spider.py
  - tests/unit/test_validate_service_structural.py
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-11]

must_haves:
  truths:
    - "`scrapy crawl validate -a shop=vaga` creates a scrape_runs row with phase='validate' and finishes status='completed'"
    - "ValidateService.run() returns counters and bulk-inserts ValidationIssue rows via `bulk_insert_validation_issues`"
    - "Both rows of an isbn_duplicate / title_author_duplicate / sku_duplicate pair receive a ValidationIssue"
    - "slug_title_mismatch fires only when the slug-title token intersection is empty"
    - "Re-running validate on identical data does not create duplicate ValidationIssue rows (lifecycle becomes `recurring`)"
    - "ValidateSpider.start() sets `self._run_id` BEFORE calling ValidateService — HeartbeatExtension picks it up"
    - "ValidateService runs off the asyncio reactor thread via `asyncio.to_thread()` — heartbeat ticks fire during long SQL"
  artifacts:
    - path: "book_scraper/services/validate.py"
      provides: "ValidateService class with structural duplicate + slug-title checks; module constant VALIDATE_STALE_CADENCE_DAYS = 14"
      contains: "class ValidateService"
    - path: "book_scraper/spiders/validate.py"
      provides: "ValidateSpider — Scrapy spider mirroring match.py; no HTTP, runs ValidateService via asyncio.to_thread()"
      contains: "class ValidateSpider"
    - path: "tests/unit/test_validate_spider.py"
      provides: "Spider invariant tests (run_id ordering, off-thread dispatch)"
      contains: "test_run_id_is_set_before_validate_service_runs"
    - path: "tests/unit/test_validate_service_structural.py"
      provides: "Pure unit tests for slug-title tokenisation"
      contains: "_tokenize"
  key_links:
    - from: "book_scraper/spiders/validate.py"
      to: "book_scraper/services/validate.py"
      via: "from book_scraper.services.validate import ValidateService"
      pattern: "from book_scraper\\.services\\.validate import ValidateService"
    - from: "book_scraper/spiders/validate.py"
      to: "book_scraper/db/repo.py"
      via: "create_scrape_run(..., 'validate', ...) and finish_scrape_run"
      pattern: "create_scrape_run.*validate"
    - from: "book_scraper/services/validate.py"
      to: "book_scraper/db/repo.py"
      via: "bulk_insert_validation_issues(session, issues, shop_id=shop_id)"
      pattern: "bulk_insert_validation_issues"
---

<objective>
Build the ValidateSpider + ValidateService skeleton with the first two check groups (structural duplicates, slug-title mismatch). The spider mirrors `book_scraper/spiders/match.py` exactly: no HTTP, no items pipeline, StallDetector disabled, ValidateService dispatched via `asyncio.to_thread()` so HeartbeatExtension keeps ticking.

Purpose: Establish the entrypoint and service shell so plan 03 can simply add more `check_*` methods. Implements the deduplication path through `bulk_insert_validation_issues` once for all subsequent checks. Adds unit tests covering the heartbeat-ordering invariant that broke MatchSpider on production runs #387/#391.

Output: Two new production files, two new test files, all tests green via `uv run pytest tests/unit/test_validate_spider.py tests/unit/test_validate_service_structural.py -v`.
</objective>

<execution_context>
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evaldas/Projects/book-scraper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-validate-phase/01-CONTEXT.md
@.planning/phases/01-validate-phase/01-RESEARCH.md
@CLAUDE.md
@docs/superpowers/specs/2026-05-10-shop-books-validate-phase-design.md
@book_scraper/spiders/match.py
@book_scraper/services/match.py
@tests/unit/test_match_spider.py

<interfaces>
<!-- Existing exports the new code consumes. All verified in research. -->

From `book_scraper/db/repo.py` (signatures only — read full file for behaviour):
```python
def create_scrape_run(
    session: Session, shop_id: int, phase: str,
    extra_payload: dict | None = None,
) -> ScrapeRun
def finish_scrape_run(session: Session, run_id: int, status: str, reason: str | None = None) -> None
def finalize_run_failsafe(session: Session, run_id: int, status: str, reason: str | None = None) -> None
def upsert_shop(session: Session, name: str, base_url: str) -> Shop
def bulk_insert_validation_issues(
    session: Session,
    issues: list[dict[str, str | int | None]],
    shop_id: int | None = None,
) -> None
```

From `book_scraper/db/models.py` (relevant ShopBook fields):
```python
class ShopBook(Base):
    id: int
    shop_id: int
    url: str
    title: str | None
    author: str | None
    sku: str | None
    isbn: str | None
    year: int | None
    format: str | None
    type: str  # 'book' | 'non_book' | 'audio' | 'ebook'
    is_active: bool
    in_stock: bool
    last_seen_at: datetime
    match_status: str  # 'unmatched' | 'matched' | ...
```

From `book_scraper/db/models.py` (ValidationIssue.url is NOT NULL — must always be populated):
```python
class ValidationIssue(Base):
    scrape_run_id: int           # FK -> scrape_runs
    url: str                     # NOT NULL — populate from shop_book.url
    field: str                   # issue key, e.g. 'isbn'
    issue: str                   # human-readable, e.g. 'isbn_duplicate'
    raw_value: str | None
    shop_book_id: int | None     # FK -> shop_books
    discovered_url_id: int | None
    lifecycle_state: str         # default 'new'
```

From `book_scraper/spiders/match.py` (exact pattern to mirror — read the file end-to-end):
- `custom_settings = {"ITEM_PIPELINES": {}, "EXTENSIONS": {"book_scraper.extensions.StallDetector": None, "book_scraper.extensions.CronChainTrigger": 520}}`
- `start()` is `async def` returning `AsyncGenerator[scrapy.Request, None]`
- Creates run row in a session, commits, captures `run_id`, closes session
- Sets `self._run_id = run_id`
- Calls `await asyncio.to_thread(_run_validate)` where `_run_validate` opens its own session
- Reopens a session to call `finish_scrape_run` and commit
- Ends with `return; yield` to satisfy AsyncGenerator typing

From `book_scraper/dashboard/extensions.py` (read it once during the task):
- `HeartbeatExtension` reads `getattr(spider, "_run_id", None)` lazily on each tick.
- DEAD_RUN_SECONDS = 60 — if heartbeat doesn't tick for 60s, dashboard reaper marks the run failed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ValidateService skeleton with structural duplicate + slug-title checks</name>
  <files>
    book_scraper/services/validate.py
    tests/unit/test_validate_service_structural.py
  </files>
  <read_first>
    - book_scraper/services/match.py (mirror its class structure, init signature, return-counters pattern)
    - book_scraper/db/repo.py lines 1500-1660 (bulk_insert_validation_issues + _assign_lifecycle_states behaviour)
    - book_scraper/db/models.py lines 89-200 (ShopBook fields)
    - book_scraper/db/models.py lines 602-642 (ValidationIssue model — confirm url is NOT NULL)
    - docs/superpowers/specs/2026-05-10-shop-books-validate-phase-design.md (severities + check definitions)
    - .planning/phases/01-validate-phase/01-RESEARCH.md (Pattern 4 SQL, Pattern 5 dispatch ordering)
  </read_first>
  <behavior>
    - `ValidateService(session)` constructs without DB I/O.
    - `svc.run(shop_id: int, run_id: int) -> dict[str, int]` returns counters keyed by issue key, e.g. `{"isbn_duplicate": 4, "slug_title_mismatch": 2, ...}`. Total inserted = sum of values.
    - `svc.check_structural_duplicates(shop_id, run_id)` returns `list[dict]` ready for `bulk_insert_validation_issues`. Each dict has keys: `scrape_run_id`, `url`, `field`, `issue`, `raw_value`, `shop_book_id`.
    - For an ISBN duplicate where rows A and B share `isbn=X`, BOTH rows get a ValidationIssue with `field='isbn'`, `issue='isbn_duplicate'`, `raw_value='X'`.
    - Same-pair logic for `title_author_duplicate` (match on lower(title) + lower(author), both non-null) and `sku_duplicate` (match on sku, non-null).
    - `_tokenize(s)` strips diacritics via `unicodedata.normalize('NFD', s.lower())` + filter `category(c) != 'Mn'`, splits on `r'[-\s]+'`, returns set minus empty string.
    - `check_slug_title_mismatch(shop_id, run_id)` flags only when both `slug_tokens` and `title_tokens` are non-empty AND their intersection is empty. Slug = `url.rstrip('/').rsplit('/', 1)[-1]`. Field='slug', issue='slug_title_mismatch', raw_value=slug.
    - Module-level constant `VALIDATE_STALE_CADENCE_DAYS = 14` is exported (used by plan 03).
    - `svc.run()` calls each check method, accumulates issue dicts, calls `bulk_insert_validation_issues(self._session, issues, shop_id=shop_id)` ONCE at the end, returns counters.
    - `svc.run()` does NOT commit — caller commits.

    Unit tests in `tests/unit/test_validate_service_structural.py` (no DB — pure logic):
    - `test_tokenize_strips_diacritics`: `_tokenize("Ką šunys galvoja?")` returns set containing tokens with diacritics stripped (e.g. `{"ka", "sunys", "galvoja?"}`-ish — assert specific tokens like `'sunys'` present, `'šunys'` absent).
    - `test_tokenize_splits_on_dash_and_space`: `_tokenize("vyresnio-amziaus zmoniu")` returns `{"vyresnio", "amziaus", "zmoniu"}`.
    - `test_tokenize_empty_string_returns_empty_set`: `_tokenize("")` returns `set()`.
    - `test_validate_stale_cadence_constant_is_14_days`: imports `VALIDATE_STALE_CADENCE_DAYS` and asserts `== 14`.
    - `test_slug_title_mismatch_zero_intersection_flags`: a helper or pure-Python predicate (extracted from the check method, e.g. `_should_flag_slug_title(slug, title) -> bool`) returns True when zero overlap.
    - `test_slug_title_mismatch_one_overlap_does_not_flag`: same helper returns False when at least one token overlaps (e.g. slug `sapiens-trumpa-zmonijos`, title `Sapiens trumpa istorija`).
    - `test_slug_title_mismatch_handles_lithuanian_diacritics`: slug `silko-kelias` and title `Šilko kelias` overlap on `silko`, returns False.
  </behavior>
  <action>
Create `book_scraper/services/validate.py` containing:

1. **Imports**: `from __future__ import annotations`, `import re`, `import unicodedata`, `from sqlalchemy import text`, `from sqlalchemy.orm import Session`, `from book_scraper.db.repo import bulk_insert_validation_issues`.

2. **Module constant**: `VALIDATE_STALE_CADENCE_DAYS: int = 14` — pending per-shop discover cadence (deferred to v2 per CONTEXT.md). Add a one-line comment pointing to that decision.

3. **Helpers** (module-level functions, NOT methods, so they can be imported by tests directly):
   ```python
   def _tokenize(s: str) -> set[str]:
       """Lowercase, strip diacritics (NFD + filter Mn category), split on -/whitespace."""
       if not s:
           return set()
       nfd = unicodedata.normalize("NFD", s.lower())
       ascii_only = "".join(c for c in nfd if unicodedata.category(c) != "Mn")
       return set(re.split(r"[-\s]+", ascii_only)) - {""}

   def _should_flag_slug_title(slug: str | None, title: str | None) -> bool:
       """True iff both have tokens AND intersection is empty."""
       if not slug or not title:
           return False
       slug_tokens = _tokenize(slug)
       title_tokens = _tokenize(title)
       if not slug_tokens or not title_tokens:
           return False
       return not (slug_tokens & title_tokens)
   ```

4. **`ValidateService` class** with:
   - `__init__(self, session: Session) -> None` — stores session as `self._session`.
   - `run(self, shop_id: int, run_id: int) -> dict[str, int]`:
     - calls `self.check_structural_duplicates(shop_id, run_id)` and `self.check_slug_title_mismatch(shop_id, run_id)`
     - extends a single `issues: list[dict]` from each
     - if `issues`: `bulk_insert_validation_issues(self._session, issues, shop_id=shop_id)`
     - builds `counters: dict[str, int] = {}` by counting `issue["issue"]` keys
     - returns counters
   - `check_structural_duplicates(self, shop_id: int, run_id: int) -> list[dict]`:
     - For ISBN: `text("SELECT sb.id, sb.url, sb.isbn FROM shop_books sb WHERE sb.shop_id = :shop_id AND sb.isbn IS NOT NULL AND EXISTS (SELECT 1 FROM shop_books sb2 WHERE sb2.shop_id = :shop_id AND sb2.isbn = sb.isbn AND sb2.id != sb.id)")`. For each row append `{"scrape_run_id": run_id, "url": row.url, "field": "isbn", "issue": "isbn_duplicate", "raw_value": row.isbn, "shop_book_id": row.id}`.
     - For title+author: same pattern but match on `lower(sb.title) = lower(sb2.title) AND lower(sb.author) = lower(sb2.author)` with `sb.title IS NOT NULL AND sb.author IS NOT NULL`. field="title_author", issue="title_author_duplicate", raw_value=`f"{title} / {author}"`.
     - For SKU: same pattern matching on sku, sku non-null. field="sku", issue="sku_duplicate", raw_value=row.sku.
   - `check_slug_title_mismatch(self, shop_id: int, run_id: int) -> list[dict]`:
     - Query: `text("SELECT id, url, title FROM shop_books WHERE shop_id = :shop_id AND title IS NOT NULL")`.
     - For each row, derive `slug = row.url.rstrip("/").rsplit("/", 1)[-1]` and call `_should_flag_slug_title(slug, row.title)`.
     - When True, append `{"scrape_run_id": run_id, "url": row.url, "field": "slug", "issue": "slug_title_mismatch", "raw_value": slug, "shop_book_id": row.id}`.
   - Type hints on every method. Docstrings reference the spec section.

5. **Tests** in `tests/unit/test_validate_service_structural.py` covering exactly the cases enumerated in `<behavior>` above. Tests import from `book_scraper.services.validate` directly, instantiate nothing that needs a DB, and exercise only `_tokenize`, `_should_flag_slug_title`, and `VALIDATE_STALE_CADENCE_DAYS`.

Reference the spec for severity assignments (info/warning/critical) — but DO NOT store severity on the ValidationIssue row; the model has no severity column. Severity is dashboard-side computed from `issue` key (per existing `book_scraper/dashboard/queries.py`); validate phase only writes the issue key and lets dashboard render it.

DO NOT call `session.commit()` inside ValidateService — the spider commits after `run()` returns (matching MatchService).
  </action>
  <verify>
    <automated>
uv run ruff check book_scraper/services/validate.py tests/unit/test_validate_service_structural.py &&
uv run mypy book_scraper/services/validate.py &&
uv run pytest tests/unit/test_validate_service_structural.py -v
    </automated>
  </verify>
  <acceptance_criteria>
    - File `book_scraper/services/validate.py` exists.
    - `grep -c "^class ValidateService" book_scraper/services/validate.py` returns 1
    - `grep -c "^VALIDATE_STALE_CADENCE_DAYS: int = 14" book_scraper/services/validate.py` returns 1
    - `grep -c "def _tokenize" book_scraper/services/validate.py` returns 1
    - `grep -c "def check_structural_duplicates" book_scraper/services/validate.py` returns 1
    - `grep -c "def check_slug_title_mismatch" book_scraper/services/validate.py` returns 1
    - `grep -c "isbn_duplicate" book_scraper/services/validate.py` >= 1
    - `grep -c "title_author_duplicate" book_scraper/services/validate.py` >= 1
    - `grep -c "sku_duplicate" book_scraper/services/validate.py` >= 1
    - `grep -c "bulk_insert_validation_issues" book_scraper/services/validate.py` returns 1
    - `grep -v '^#' book_scraper/services/validate.py | grep -c "session.commit"` returns 0 (service must NOT commit)
    - `uv run pytest tests/unit/test_validate_service_structural.py -v` reports >= 7 passing tests, 0 failures.
    - `uv run mypy book_scraper/services/validate.py` exits 0.
    - `uv run ruff check book_scraper/services/validate.py tests/unit/test_validate_service_structural.py` exits 0.
  </acceptance_criteria>
  <done>ValidateService class exists with structural-duplicate + slug-title-mismatch implementations, all unit tests pass, mypy + ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ValidateSpider mirroring match.py + spider invariant unit tests</name>
  <files>
    book_scraper/spiders/validate.py
    tests/unit/test_validate_spider.py
  </files>
  <read_first>
    - book_scraper/spiders/match.py (entire file — verbatim template; ValidateSpider is structurally identical to MatchSpider apart from name + service class)
    - book_scraper/spiders/scan.py (only the `closed()` method — read for the `finalize_run_failsafe` failsafe pattern)
    - tests/unit/test_match_spider.py (entire file — clone its fixture pattern + invariant tests)
    - book_scraper/extensions.py (HeartbeatExtension._resolve_run_id — confirms the `_run_id` lazy-read invariant)
    - book_scraper/services/validate.py (the file Task 1 just created — tests need to patch `book_scraper.spiders.validate.ValidateService`)
  </read_first>
  <behavior>
    - `ValidateSpider.name = "validate"`.
    - `custom_settings` disables `book_scraper.extensions.StallDetector` (set to None) and pins `book_scraper.extensions.CronChainTrigger: 520`. ITEM_PIPELINES emptied.
    - `__init__(self, shop=None)` raises ValueError when `shop` is missing, sets `self.shop_name`, loads TOML config via `load_shop_config`.
    - `start()` is async, opens a session, calls `upsert_shop`, calls `create_scrape_run(session, shop.id, "validate", extra_payload={"shop": self.shop_name})`, commits, captures `run_id`, closes session.
    - Sets `self._run_id = run_id` BEFORE invoking the service (heartbeat ordering invariant).
    - Dispatches the service via `await asyncio.to_thread(_run_validate)` where `_run_validate` opens its own session, instantiates ValidateService, calls `.run(shop.id, run_id)`, commits, returns counters.
    - On exception inside `_run_validate`: rollback session, re-raise; `closed()` callback then calls `finalize_run_failsafe(..., status="failed")` — same fall-through pattern as `scan.py`.
    - On success: reopen session, call `finish_scrape_run(session, run_id, status="completed")`, commit.
    - `closed(self, reason: str)` callback: open session, call `finalize_run_failsafe(session, self._run_id, "failed", reason=reason)` only if `_run_id` exists; commit; close. Always wrapped in try/except that swallows + logs (reaper is the last-resort backstop).
    - Spider yields no requests; the trailing `return; yield` pattern from `match.py` is preserved.

    Unit tests (clone the structure of `tests/unit/test_match_spider.py` exactly):
    - `test_run_id_is_set_before_validate_service_runs`: ValidateService.run is patched to record `getattr(spider, "_run_id", None)`; assert it equals the fake run id (NOT None).
    - `test_validate_service_runs_off_main_thread`: ValidateService.run is patched to record `threading.current_thread()`; assert it is NOT the main thread (proves `asyncio.to_thread` dispatch).
    - `test_finish_scrape_run_called_with_completed_on_success`: assert `finish_scrape_run` called once with `status="completed"`.
    - `test_validate_service_invoked_with_shop_id_and_run_id`: assert `ValidateService.return_value.run` called with positional args `(fake_shop.id, fake_run.id)`.
    - `test_missing_shop_argument_raises`: `ValidateSpider()` (no shop) raises ValueError.
  </behavior>
  <action>
Create `book_scraper/spiders/validate.py` by cloning `book_scraper/spiders/match.py` with these edits:
- Module docstring rewritten for validate phase.
- `from book_scraper.services.match import MatchService` -> `from book_scraper.services.validate import ValidateService`.
- `from book_scraper.db.repo import (..., finalize_run_failsafe, ...)` — add `finalize_run_failsafe` to the import list (match.py omits it; validate adds the closed() failsafe).
- `class MatchSpider(scrapy.Spider)` -> `class ValidateSpider(scrapy.Spider)`.
- `name = "match"` -> `name = "validate"`.
- Inside `start()`:
  - `create_scrape_run(session, shop.id, "match", ...)` -> `create_scrape_run(session, shop.id, "validate", ...)`
  - `MatchService(s).run(self.shop_name)` -> `ValidateService(s).run(shop.id, run_id)` — IMPORTANT: validate's signature takes shop_id + run_id, not shop_name. Capture `shop.id` in the outer scope so the closure can use it.
  - `counters` is a dict, not a dataclass — drop the `run.items_updated = counters.total_updates` line (validate counters don't map to items_updated).
- Add a `closed(self, reason: str = "")` synchronous callback (Scrapy calls it on spider shutdown). Inside it:
  ```python
  run_id = getattr(self, "_run_id", None)
  if run_id is None:
      return
  database_url = self.settings.get("DATABASE_URL") if hasattr(self, "settings") else None
  if not database_url:
      return
  session = get_session_factory(database_url)()
  try:
      finalize_run_failsafe(session, run_id, "failed", reason=reason or "spider_closed")
      session.commit()
  except Exception:
      session.rollback()
  finally:
      session.close()
  ```
  Note: `finalize_run_failsafe` is idempotent — it only updates rows still in 'running' status, so calling it after a successful `finish_scrape_run` is a no-op (verified in repo.py).

Create `tests/unit/test_validate_spider.py` by cloning `tests/unit/test_match_spider.py` with these edits:
- Replace `MatchSpider` -> `ValidateSpider`, `MatchService` -> `ValidateService`, `MatchCounters` -> `dict` (validate returns a plain dict).
- Replace patch targets: `book_scraper.spiders.match.X` -> `book_scraper.spiders.validate.X` for each of `get_session_factory`, `upsert_shop`, `create_scrape_run`, `finish_scrape_run`, `ValidateService`.
- The fixture's `ms_cls.return_value.run.side_effect = lambda shop_name: counters` becomes `vs_cls.return_value.run.side_effect = lambda shop_id, run_id: counters` (positional args match new signature).
- Test `test_counters_propagate_to_items_updated` from match — DROP it (validate doesn't update `items_updated`).
- Add `test_missing_shop_argument_raises`: instantiate `ValidateSpider()` directly (no shop kwarg) and assert ValueError. Note this test cannot use the `_build_spider` shortcut because the shortcut bypasses `__init__`; use `pytest.raises(ValueError)` around `ValidateSpider()`.

Make sure imports include `import threading` if needed for the off-main-thread test.
  </action>
  <verify>
    <automated>
uv run ruff check book_scraper/spiders/validate.py tests/unit/test_validate_spider.py &&
uv run mypy book_scraper/spiders/validate.py &&
uv run pytest tests/unit/test_validate_spider.py tests/unit/test_validate_service_structural.py -v
    </automated>
  </verify>
  <acceptance_criteria>
    - File `book_scraper/spiders/validate.py` exists.
    - `grep -c "^class ValidateSpider" book_scraper/spiders/validate.py` returns 1
    - `grep -c 'name = "validate"' book_scraper/spiders/validate.py` returns 1
    - `grep -c "book_scraper.extensions.StallDetector.*None" book_scraper/spiders/validate.py` returns 1
    - `grep -c "ITEM_PIPELINES" book_scraper/spiders/validate.py` returns 1
    - `grep -c "asyncio.to_thread" book_scraper/spiders/validate.py` returns 1
    - `grep -c 'self._run_id = run_id' book_scraper/spiders/validate.py` returns 1
    - `grep -c "from book_scraper.services.validate import ValidateService" book_scraper/spiders/validate.py` returns 1
    - `grep -c 'create_scrape_run.*"validate"' book_scraper/spiders/validate.py` returns 1
    - `grep -c "finalize_run_failsafe" book_scraper/spiders/validate.py` >= 1
    - `grep -c "def closed" book_scraper/spiders/validate.py` returns 1
    - File `tests/unit/test_validate_spider.py` exists with >= 5 test functions (`grep -c "^def test_" tests/unit/test_validate_spider.py` >= 5)
    - `uv run pytest tests/unit/test_validate_spider.py -v` reports >= 5 passing, 0 failures.
    - `uv run pytest tests/unit/test_validate_service_structural.py -v` still passes (regression guard).
    - `uv run mypy book_scraper/spiders/validate.py` exits 0.
    - `uv run ruff check book_scraper/spiders/validate.py tests/unit/test_validate_spider.py` exits 0.
  </acceptance_criteria>
  <done>ValidateSpider exists, mirrors match.py with the validate-specific service signature + a closed() failsafe; spider unit tests pass and assert the heartbeat-ordering + off-thread invariants.</done>
</task>

</tasks>

<verification>
- Both new test files run green: `uv run pytest tests/unit/test_validate_spider.py tests/unit/test_validate_service_structural.py -v`.
- `uv run mypy book_scraper/services/validate.py book_scraper/spiders/validate.py` exits 0.
- `uv run ruff check book_scraper/services/validate.py book_scraper/spiders/validate.py tests/unit/test_validate_spider.py tests/unit/test_validate_service_structural.py` exits 0.
- The skeleton is sufficient for plan 03 to extend ValidateService with five more `check_*` methods without touching the spider.
</verification>

<success_criteria>
- VAL-01 satisfied (skeleton): ValidateService runs DB queries and writes via `bulk_insert_validation_issues`. (Two of seven check groups implemented here; remaining five in plan 03.)
- VAL-02 satisfied: Spider creates a `scrape_runs` row with `phase='validate'` and finalises it via `finish_scrape_run` / `finalize_run_failsafe`.
- VAL-03 satisfied: All three structural duplicate checks implemented, both rows of each pair flagged.
- VAL-04 satisfied: slug_title_mismatch with zero-token-overlap threshold + diacritic stripping.
- VAL-11 satisfied: Deduplication delegated to `_assign_lifecycle_states` via `bulk_insert_validation_issues`.
- Heartbeat-ordering invariant locked in by unit test (matches MatchSpider's regression coverage).
</success_criteria>

<output>
After completion, create `.planning/phases/01-validate-phase/01-02-SUMMARY.md` documenting:
- Two production files added + two test files added
- Confirmation that ValidateService has the four module-level/method symbols future plans will extend (`run`, `check_structural_duplicates`, `check_slug_title_mismatch`, `VALIDATE_STALE_CADENCE_DAYS`)
- Test counts (unit only — integration tests come in plan 03)
- Note any divergence from match.py's pattern (e.g. closed() callback)
</output>
